# DO NOT EDIT — generated by scripts/render-manifests.sh from argocd/envs/prod/airflow.tmpl.yaml
# Source of truth: edit the .tmpl.yaml then run scripts/render-manifests.sh
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: airflow
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: openvelox
spec:
  project: default
  sources:
    - chart: airflow
      repoURL: https://airflow.apache.org
      targetRevision: 1.20.0
      helm:
        valueFiles:
          - $values/helm/airflow/values-gke.yaml
          - $values/helm/airflow/values-prod.yaml
    - repoURL: https://github.com/ettysekhon/openvelox.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: batch
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
