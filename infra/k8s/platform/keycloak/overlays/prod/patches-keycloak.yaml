# DO NOT EDIT — generated by scripts/render-manifests.sh from infra/k8s/platform/keycloak/overlays/prod/patches-keycloak.tmpl.yaml
# Source of truth: edit the .tmpl.yaml then run scripts/render-manifests.sh
apiVersion: apps/v1
kind: Deployment
metadata:
  name: keycloak
  namespace: platform
spec:
  selector:
    matchLabels:
      app: keycloak
  template:
    spec:
      containers:
        - name: keycloak
          env:
            - name: KC_HOSTNAME
              value: "https://auth.metrovelox.com"
            - name: KC_HOSTNAME_ADMIN
              value: "https://auth.metrovelox.com"
