apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  # Declarative raw/curated/analytics SQL submissions — replaces the
  # ad-hoc `kubectl exec sql-client.sh` flow used during the initial
  # soak. See ./flink-sql-jobs/README.md for the layout + flow.
  - ./flink-sql-jobs
patches:
  - path: patches-oauth2-proxy.yaml
  - path: patches-tfl-producer-strimzi.yaml
  - path: patches-flink-session.yaml
