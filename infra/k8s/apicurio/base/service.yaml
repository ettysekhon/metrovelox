apiVersion: v1
kind: Service
metadata:
  name: apicurio-registry
  namespace: kafka
  labels:
    app.kubernetes.io/name: apicurio-registry
    app.kubernetes.io/part-of: openvelox
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: apicurio-registry
  ports:
    - name: http
      port: 8080
      targetPort: 8080
