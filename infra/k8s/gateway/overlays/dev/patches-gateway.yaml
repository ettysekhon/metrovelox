apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: openvelox-gateway
  namespace: platform
  annotations:
    networking.gke.io/certmap: dev-openvelox-certmap
spec:
  gatewayClassName: gke-l7-global-external-managed
