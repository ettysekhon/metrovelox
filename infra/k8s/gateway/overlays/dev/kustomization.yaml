# DO NOT EDIT — generated by scripts/render-manifests.sh from infra/k8s/gateway/overlays/dev/kustomization.tmpl.yaml
# Source of truth: edit the .tmpl.yaml then run scripts/render-manifests.sh
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - path: patches-gateway.yaml
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: keycloak-route
        namespace: platform
      spec:
        hostnames: ["dev.auth.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: argocd-route
        namespace: argocd
      spec:
        hostnames: ["dev.argocd.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: orchestrator-route
        namespace: batch
      spec:
        hostnames: ["dev.orchestrator.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: query-engine-route
        namespace: data
      spec:
        hostnames: ["dev.query.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: vault-route
        namespace: security
      spec:
        hostnames: ["dev.vault.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: catalog-route
        namespace: data
      spec:
        hostnames: ["dev.catalog.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: catalog-console-route
        namespace: data
      spec:
        hostnames: ["dev.catalog-console.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: authz-route
        namespace: security
      spec:
        hostnames: ["dev.authz.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: stream-processing-route
        namespace: streaming
      spec:
        hostnames: ["dev.stream-processing.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: grafana-route
        namespace: monitoring
      spec:
        hostnames: ["dev.grafana.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: api-route
        namespace: apps
      spec:
        hostnames: ["dev.api.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: frontend-route
        namespace: apps
      spec:
        hostnames: ["dev.metrovelox.com"]
