# DO NOT EDIT — generated by scripts/render-manifests.sh from infra/k8s/gateway/overlays/prod/kustomization.tmpl.yaml
# Source of truth: edit the .tmpl.yaml then run scripts/render-manifests.sh
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - path: patches-gateway.yaml
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: keycloak-route
        namespace: platform
      spec:
        hostnames: ["auth.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: argocd-route
        namespace: argocd
      spec:
        hostnames: ["argocd.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: orchestrator-route
        namespace: batch
      spec:
        hostnames: ["orchestrator.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: query-engine-route
        namespace: data
      spec:
        hostnames: ["query.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: kafka-ui-route
        namespace: kafka
      spec:
        hostnames: ["kafka.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: vault-route
        namespace: security
      spec:
        hostnames: ["vault.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: catalog-route
        namespace: data
      spec:
        hostnames: ["catalog.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: catalog-console-route
        namespace: data
      spec:
        hostnames: ["catalog-console.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: authz-route
        namespace: security
      spec:
        hostnames: ["authz.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: stream-processing-route
        namespace: streaming
      spec:
        hostnames: ["stream-processing.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: grafana-route
        namespace: monitoring
      spec:
        hostnames: ["grafana.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: api-route
        namespace: apps
      spec:
        hostnames: ["api.metrovelox.com"]
  - patch: |
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        name: frontend-route
        namespace: apps
      spec:
        hostnames: ["metrovelox.com"]
