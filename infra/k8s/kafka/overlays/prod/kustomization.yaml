apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kafka
resources:
  - ../../base
# Prod-grade hardening (3-replica KafkaNodePool, RF=3, min-ISR=2, cert-manager
# TLS listener, PodDisruptionBudgets) lands in a follow-up PR — see
# docs/ROADMAP.md §7 for the current gap. This overlay exists now so the
# ArgoCD app structure does not need reshaping then.
