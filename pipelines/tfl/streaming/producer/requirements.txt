httpx>=0.27.0
kafka-python-ng>=2.2.0
# librdkafka-backed client — needed for OAUTHBEARER against Strimzi. The
# existing Redpanda path still uses kafka-python-ng (no SASL today).
confluent-kafka>=2.5.0
